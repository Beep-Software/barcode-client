apiVersion: v1
kind: Service
metadata:
  name: barcode-client
  namespace: default
spec:
  type: NodePort
  selector:
    app: barcode-client
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30000